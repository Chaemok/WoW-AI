# Runpod A40 같은 서버 환경에서 추론용으로 사용하는 의존성 목록
# PyTorch 템플릿에 torch가 이미 설치되어 있다는 전제를 둔다.
# torch 재설치가 필요하면 requirements.txt를 사용하거나 별도로 설치한다.

# 서버 실행
Flask==3.1.3
requests>=2.28.1
python-dotenv==1.2.2

# 데이터 처리 / GMM 추론
pandas==3.0.1
numpy==2.4.3
joblib==1.5.3
scikit-learn==1.8.0

# 엑셀 업로드 지원
openpyxl==3.1.5
xlrd==2.0.2

# LLM 추론
transformers>=4.47.1,<6
accelerate==1.13.0
bitsandbytes
sentencepiece==0.2.1
protobuf>=3.20
